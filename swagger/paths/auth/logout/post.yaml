tags:
  - Auth
summary: Logout current user
operationId: logoutUser
description: "Logout current user"
security:
  - bearerAuth: []
parameters:
  - in: cookie
    name: sessionId
    required: true
    schema:
      type: string
responses:
  "204":
    description: "Successfully log out a user"
